// 注意：CSS 文件需要单独导入
// 如果用户有自己的 Tailwind CSS，只需导入组件：
//   import { Button } from "@template/template";
// 如果用户想使用包内的 CSS，需要额外导入：
//   import "@template/template/index.css";
export * from "@/components";